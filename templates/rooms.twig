{% extends 'origin.twig' %}

 {% block style %}
     {{ parent() }}
     <link rel="stylesheet" href="../css/lists.css">
 {% endblock %}

    {% block script %}
        {{ parent() }}
        <script src="../js/modal.js"></script>
    {% endblock %}

{% block main %}
    <main>
        <h1>Lista habitaciones</h1>
        {% if rooms|length > 0 %}
            <div class="table_list three_items">
                <div class="element">
                    <span class="border odd">Número</span>
                    <span class="border odd">Precio noche</span>
                    <span class="border odd">Capacidad</span>
                </div>

                {% for room in rooms %}
                    <div class="element" onclick="detailsWindow('{{ room.id }}')">
                        <span class="{{ loop.index % 2 == 0 ? 'odd' : 'even' }}">{{ room.room_num }}</span>
                        <span class="{{ loop.index % 2 == 0 ? 'odd' : 'even' }}">{{ room.price }}€</span>
                        <span class="{{ loop.index % 2 == 0 ? 'odd' : 'even' }}">{{ room.capacity }}</span>

                        <div id="{{ room.id }}" class="container container_three {{ loop.index % 2 == 0 ? 'odd' : 'even' }}">
                            {% if room.images|length > 0 %}
                                <h2>Fotos de la habitación</h2>
                                <div class="images">
                                    {% for image in room.images %}
                                        <img src="data:image/jpeg;base64,{{ image.image }}" alt="imagen de la habitación">
                                    {% endfor %}
                                </div>
                            {% endif %}
                            <h2>Descripción</h2>
                            <p>{{ room.description }}</p>
                        </div>
{#                        <div class="operations {{ loop.index % 2 == 0 ? 'odd' : 'even' }}">#}
{#                            <a href="createUser.php?id={{ user.id }}">#}
{#                                <img src="../images/edit.png" class="image_button" alt="icono para editar usuario"/>#}
{#                            </a>#}
{#                            <img src="../images/delete.png" class="image_button" alt="icono para borrar usuario" onclick="generateDeleteUserModal({{ user.id }},'{{user.email }}')"/>#}
{#                        </div>#}
                    </div>
                {% endfor %}


            </div>
        {% else %}
            <p class="empty">Actualmente no hay usuarios registrados</p>
        {% endif %}
    </main>
{% endblock %}