{% extends 'origin.twig' %}

    {% block style %}
        {{ parent() }}
        <link rel="stylesheet" href="../css/profile.css">
    {% endblock %}

    {% block main %}
        <main>
            <h1>Perfil del Usuario</h1>
            <div class="user_data">
                <p>Nombre: {{ user.name }}</p>
                <p>Apellidos: {{ user.lastname }}</p>
                <p>DNI: {{ user.dni }}</p>
                <p>Correo: {{ user.email }}</p>
                <p>Nº tarjeta: {{ user.card }}</p>
            </div>
            {% if success is defined %}
                <p class="success">{{ success }}</p>
            {% endif %}
            <a class="redirection" href="user_data.php?profile">Modificar datos</a>
        </main>
    {% endblock %}